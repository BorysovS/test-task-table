import { Navigation } from "../Navigation/Navigatiron"
import { Header } from "./AppBar.styled"

export const AppBar = () => {
    return (
        <Header>
            <Navigation />
        </Header>
    )
}